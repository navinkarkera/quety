{
  "name": "quety",
  "version": "0.0.1",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "compile": "rm ./dist/quety; bun build --compile --minify --production --sourcemap ./src --outfile ./dist/quety",
    "lint": "eslint .",
    "lint:fix": "npm run lint --fix"
  },
  "dependencies": {
    "better-auth": "^1.3.4",
    "drizzle-orm": "^0.44.4",
    "hono": "^4.8.12",
    "hono-pino": "^0.10.1",
    "pino": "^9.7.0",
    "pino-pretty": "^13.1.1"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^5.1.0",
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.4",
    "eslint": "^9.32.0",
    "eslint-plugin-format": "^1.0.1",
    "typed-htmx": "^0.3.1"
  }
}
